# Point this at wherever your backend is actually running.
# Default FastAPI/uvicorn port is 8000, but on some Windows machines with
# security software (e.g. Quick Heal) blocking certain ports, a different
# port may be required - see README Troubleshooting for details.
VITE_API_BASE_URL=http://localhost:8000
